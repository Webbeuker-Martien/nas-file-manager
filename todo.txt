Desktop:
- Make different UI for desktop (with checkboxes for files and dirs)

Phone:
- Long press file to select multiple

Both:
- Ability to view files:
    - already done: image, video
- Add other actions (share, copy, move, upload, etc.)
- Miss lazy load components (file component, is render-blocking)
- Audio player (maybe as a separate host, or seperate options under hosts) with albums (json file with file names) etc. 